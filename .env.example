# Firebase emulator — copy to .env and fill in values

# Used by seedRoles.ts when NODE_ENV=staging or production
GOOGLE_CLOUD_PROJECT=
FIRESTORE_DATABASE=(default)

# Emulator host — set automatically by seedRoles.ts for NODE_ENV=development
# FIRESTORE_EMULATOR_HOST=localhost:8083
